const { resolve } = require('path');
module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'),//Apontando para o arquivo 'database.js' que ensta dentro da pasta config, que está dentro da pasta src, que está na raiz da pasta(o __dirname)
    'models-path': resolve(__dirname, 'src', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds')
}